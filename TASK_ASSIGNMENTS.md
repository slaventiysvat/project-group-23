# ЗАВДАННЯ ДЛЯ РОЗРОБНИКІВ - Персональний помічник

## 📋 Розподіл завдань по модулях

Цей документ містить структуровані завдання для команди розробників. Кожне завдання є самостійним модулем з чіткими вимогами та критеріями прийняття.

---

## 🏆 ЗАВДАННЯ #1: БАЗОВІ ПОЛЯ ТА ВАЛІДАЦІЯ
**Файл**: `personal_assistant/models/field.py`  
**Розробник**: Backend Developer  
**Пріоритет**: ВИСОКИЙ (блокуючий для інших завдань)  
**Час виконання**: 2-3 дні

### Технічне завдання:
Створити систему базових полів з валідацією для всіх типів даних у програмі.

### Що потрібно реалізувати:

#### 1. Базовий клас Field
```python
class Field:
    def __init__(self, value: str):
        # Ініціалізація з автоматичною валідацією
        
    def validate(self, value: str) -> str:
        # Базова валідація (не порожнє, без зайвих пробілів)
        
    def __str__(self) -> str:
        # Представлення для користувача
        
    def __repr__(self) -> str:
        # Технічне представлення для debug
```

#### 2. Клас Name (наслідує Field)
- ✅ Підтримка українських та латинських літер
- ✅ Дозволені: літери, пробіли, дефіси, апострофи  
- ✅ Регекс: `^[a-zA-Zа-яА-ЯіІїЇєЄ'\s\-]+$`
- ✅ Автоматичне перетворення в Title Case

#### 3. Клас Phone (наслідує Field)  
- ✅ Формати: +380XXXXXXXXX, 380XXXXXXXXX, 0XXXXXXXXX
- ✅ Нормалізація до +380XXXXXXXXX
- ✅ Валідація українських номерів

#### 4. Клас Email (наслідує Field)
- ✅ RFC-сумісна валідація
- ✅ Приведення до нижнього регістру
- ✅ Підтримка міжнародних символів

#### 5. Клас Birthday (наслідує Field)
- ✅ Формати: DD.MM.YYYY, DD-MM-YYYY, DD/MM/YYYY
- ✅ Заборона майбутніх дат
- ✅ Обмеження по віку (не більше 150 років)
- ✅ Метод `to_date()` для перетворення в datetime

#### 6. Клас Address (наслідує Field)
- ✅ Мінімум 5 символів
- ✅ Видалення зайвих пробілів

### Критерії прийняття:
- [ ] Всі класи успадковуються від Field
- [ ] Валідація викидає ValueError з чіткими повідомленнями українською
- [ ] Покриття тестами всіх валідаторів
- [ ] Docstrings для всіх класів та методів
- [ ] Дотримання PEP 8

### Тестові сценарії:
```python
# Позитивні тести
name = Name("Іван Петров")  # OK
phone = Phone("+380501234567")  # OK  
email = Email("test@example.com")  # OK
birthday = Birthday("15.03.1990")  # OK

# Негативні тести  
Name("")  # ValueError
Phone("123")  # ValueError
Email("invalid")  # ValueError
Birthday("32.13.2000")  # ValueError
```

---

## 🗄️ ЗАВДАННЯ #2: ФАЙЛОВЕ СХОВИЩЕ
**Файл**: `personal_assistant/storage/file_storage.py`  
**Розробник**: Backend Developer  
**Пріоритет**: ВИСОКИЙ  
**Час виконання**: 2-3 дні

### Технічне завдання:
Створити надійну систему збереження даних у JSON з backup механізмом.

### Що потрібно реалізувати:

#### Клас FileStorage
```python
class FileStorage:
    def __init__(self, data_dir: str = "data"):
        # Ініціалізація з створенням папки
        
    def save_data(self, filename: str, data: Any) -> None:
        # Збереження з backup системою
        
    def load_data(self, filename: str) -> Any:
        # Завантаження з відновленням з backup
        
    def file_exists(self, filename: str) -> bool:
        # Перевірка існування файлу
        
    def delete_file(self, filename: str) -> bool:
        # Видалення файлу та backup
        
    def get_storage_info(self) -> Dict[str, Any]:
        # Інформація про сховище
        
    def clear_all_data(self) -> bool:
        # Очищення всіх даних
```

### Особливості реалізації:

#### Backup система:
- Перед збереженням створювати `.backup` файл
- При помилці завантаження відновлювати з backup
- Atomic операції (або повністю, або ніяк)

#### Обробка помилок:
- JSON parse errors з відновленням
- File system errors з інформативними повідомленнями
- UTF-8 encoding для української мови

### Критерії прийняття:
- [ ] Транзакційне збереження з backup
- [ ] Автоматичне відновлення при помилках
- [ ] UTF-8 підтримка
- [ ] Інформативні помилки
- [ ] 100% покриття тестами

### Тестові сценарії:
```python
storage = FileStorage("test_data")

# Збереження та завантаження
storage.save_data("test", {"key": "значення"})
data = storage.load_data("test")  # {"key": "значення"}

# Відновлення з backup при пошкодженому файлі
# Тестування великих файлів
# Тестування спеціальних символів
```

---

## 👤 ЗАВДАННЯ #3: МОДЕЛЬ КОНТАКТУ
**Файл**: `personal_assistant/models/contact.py`  
**Розробник**: Backend Developer  
**Пріоритет**: СЕРЕДНІЙ (залежить від завдання #1)  
**Час виконання**: 3-4 дні

### Технічне завдання:
Створити модель контакту з повним lifecycle управлінням.

### Структура класу:
```python
class Contact:
    name: Name                    # Обов'язкове
    phones: List[Phone]          # Список телефонів
    emails: List[Email]          # Список emails
    birthday: Optional[Birthday] # День народження
    address: Optional[Address]   # Адреса
```

### Обов'язкові методи:

#### Телефони:
- `add_phone(phone: str) -> None`
- `remove_phone(phone: str) -> bool`
- `edit_phone(old_phone: str, new_phone: str) -> None`
- `find_phone(phone: str) -> Optional[Phone]`

#### Email:
- `add_email(email: str) -> None`
- `remove_email(email: str) -> bool`

#### Особиста інформація:
- `set_birthday(birthday: str) -> None`
- `remove_birthday() -> None`
- `set_address(address: str) -> None`
- `remove_address() -> None`

#### Розрахунки:
- `days_to_birthday() -> Optional[int]` - дні до наступного ДН

#### Серіалізація:
- `to_dict() -> Dict[str, Any]` - для JSON
- `from_dict(data: Dict[str, Any]) -> 'Contact'` - з JSON (classmethod)

#### Спеціальні методи:
- `__str__() -> str` - красивий вивід для користувача
- `__repr__() -> str` - для debug  
- `__eq__(other) -> bool` - порівняння за ім'ям
- `__hash__() -> int` - для set/dict

### Критерії прийняття:
- [ ] Валідація через Field класи
- [ ] Запобігання дублюванню телефонів/email
- [ ] Правильний розрахунок днів до ДН
- [ ] Красивий форматований вивід
- [ ] Серіалізація/десеріалізація
- [ ] Покриття тестами

---

## 📝 ЗАВДАННЯ #4: МОДЕЛЬ НОТАТКИ
**Файл**: `personal_assistant/models/note.py`  
**Розробник**: Backend Developer  
**Пріоритет**: СЕРЕДНІЙ  
**Час виконання**: 3-4 дні

### Технічне завдання:
Створити модель нотатки з системою тегів та метаданими.

### Структура класу:
```python
class Note:
    title: str              # Заголовок
    content: str           # Зміст
    tags: Set[str]         # Теги (унікальні)
    created_at: datetime   # Створено
    updated_at: datetime   # Оновлено
```

### Обов'язкові методи:

#### Управління змістом:
- `set_title(title: str) -> None`
- `set_content(content: str) -> None`

#### Система тегів:
- `add_tag(tag: str) -> None` - з валідацією
- `remove_tag(tag: str) -> bool`
- `has_tag(tag: str) -> bool`
- `clear_tags() -> None`

#### Пошук:
- `search_in_content(query: str, case_sensitive: bool) -> bool`
- `matches_tags(tags: List[str]) -> bool`

#### Аналітика:
- `get_word_count() -> int` - підрахунок слів

### Валідація тегів:
- Регекс: `^[a-zA-Zа-яА-ЯіІїЇєЄ0-9_\-]+$`
- Максимум 30 символів
- Нижній регістр
- Без дублікатів

### Критерії прийняття:
- [ ] Автооновлення updated_at при змінах
- [ ] Валідація заголовків та тегів
- [ ] Ефективний пошук по тексту
- [ ] Серіалізація з датами в ISO format
- [ ] Покриття тестами всіх методів

---

## 📞 ЗАВДАННЯ #5: МЕНЕДЖЕР КОНТАКТІВ
**Файл**: `personal_assistant/managers/contact_manager.py`  
**Розробник**: Backend Developer  
**Пріоритет**: СЕРЕДНІЙ (залежить від #2, #3)  
**Час виконання**: 4-5 днів

### Технічне завдання:
Створити повнофункціональний менеджер для управління контактами.

### Основні можливості:

#### CRUD операції:
- `add_contact(contact: Contact) -> None`
- `remove_contact(name: str) -> bool`
- `find_contact(name: str) -> Optional[Contact]`
- `update_contact(name: str, **kwargs) -> Optional[Contact]`

#### Пошук та фільтрація:
- `search_contacts(query: str) -> List[Contact]` - по всіх полях
- `get_all_contacts(sort_by: str) -> List[Contact]` - з сортуванням
- `get_upcoming_birthdays(days_ahead: int) -> List[Contact]`

#### Аналітика:
- `get_statistics() -> Dict[str, Any]` - детальна статистика

### Особливості:
- Внутрішнє зберігання: `Dict[str, Contact]` (ключ = ім'я.lower())
- Автозбереження після кожної зміни
- Пошук за частковим збігом
- Сортування за різними критеріями

### Критерії прийняття:
- [ ] Швидкий пошук (O(1) за ім'ям)
- [ ] Flexble пошук по всіх полях
- [ ] Правильні розрахунки днів народження
- [ ] Автоматичне збереження/завантаження
- [ ] Статистика з усіма метриками

---

## 📚 ЗАВДАННЯ #6: МЕНЕДЖЕР НОТАТОК
**Файл**: `personal_assistant/managers/note_manager.py`  
**Розробник**: Backend Developer  
**Пріоритет**: СЕРЕДНІЙ (залежить від #2, #4)  
**Час виконання**: 4-5 днів

### Технічне завдання:
Створити менеджер нотаток з потужною системою тегів.

### Основні можливості:

#### CRUD:
- `create_note(title, content, tags) -> Note`
- `remove_note(index: int) -> bool`
- `update_note(index: int, **kwargs) -> Optional[Note]`

#### Пошук:
- `search_notes(query: str) -> List[Tuple[int, Note]]`
- `find_notes_by_tags(tags: List[str], match_all: bool) -> List[Tuple[int, Note]]`
- `find_notes_by_title(title: str) -> List[Tuple[int, Note]]`

#### Теги:
- `get_all_tags() -> Set[str]`
- `get_tag_statistics() -> Dict[str, int]`
- `add_tag_to_note(index: int, tag: str) -> bool`

#### Сортування:
- За датою створення/оновлення
- За заголовком
- За кількістю тегів

### Особливості:
- Індекси для користувача починаються з 1
- Повертаються tuple (index, note)
- Гнучкий пошук за тегами (AND/OR логіка)

### Критерії прийняття:
- [ ] Ефективний full-text пошук
- [ ] Потужна система тегів
- [ ] Flexble сортування
- [ ] Правильна індексація для UI
- [ ] Статистика використання тегів

---

## 🤖 ЗАВДАННЯ #7: РОЗПІЗНАВАННЯ КОМАНД
**Файл**: `personal_assistant/utils/command_matcher.py`  
**Розробник**: AI/ML Developer або Senior Backend  
**Пріоритет**: СЕРЕДНІЙ  
**Час виконання**: 5-6 днів

### Технічне завдання:
Створити інтелектуальний розпізнавач команд з підтримкою природної мови.

### Алгоритми розпізнавання:

#### 1. Точний збіг (100% confidence)
- Словник псевдонімів для кожної команди

#### 2. Pattern matching (90% confidence)
- Регулярні вирази для команд
- Підтримка української/англійської мов

#### 3. Keyword matching (змінна confidence)
- Пошук ключових слів у тексті
- Підрахунок релевантності

#### 4. Fuzzy search (50-90% confidence)  
- difflib для нечіткого пошуку
- Обробка друкарських помилок

### Підтримувані команди:
```
add_contact, search_contact, show_contacts, edit_contact, delete_contact, birthdays
add_note, search_notes, show_notes, edit_note, delete_note, notes_by_tags  
help, exit, statistics
```

### Основні методи:
- `find_best_command(input: str) -> Tuple[str, float]`
- `suggest_commands(input: str) -> List[Tuple[str, float]]`
- `get_command_description(command: str) -> str`

### Критерії прийняття:
- [ ] 90%+ точність розпізнавання
- [ ] Обробка друкарських помилок
- [ ] Підтримка двох мов
- [ ] Швидка робота (< 100ms)
- [ ] Configurable thresholds

---

## 🔧 ЗАВДАННЯ #8: ВАЛІДАТОРИ ТА УТИЛІТИ
**Файл**: `personal_assistant/utils/validators.py`  
**Розробник**: Junior/Middle Developer  
**Пріоритет**: НИЗЬКИЙ  
**Час виконання**: 2-3 дні

### Технічне завдання:
Створити набір допоміжних функцій для валідації та форматування.

### Функції валідації:
- `validate_input_not_empty(value, field_name) -> str`
- `validate_positive_integer(value, field_name) -> int`
- `validate_choice_from_list(value, choices) -> str`
- `validate_yes_no(value) -> bool`
- `validate_tags_input(tags_str) -> List[str]`

### Функції форматування:
- `format_list_for_display(items) -> str`
- `truncate_text(text, max_length) -> str`
- `highlight_search_term(text, term) -> str`
- `parse_command_with_args(input_str) -> Tuple[str, List[str]]`

### Критерії прийняття:
- [ ] Всі функції покриті тестами
- [ ] Чіткі error messages українською  
- [ ] Docstrings з прикладами
- [ ] Оптимізація для швидкості

---

## 🖥️ ЗАВДАННЯ #9: CLI ІНТЕРФЕЙС
**Файл**: `personal_assistant/cli.py`  
**Розробник**: Senior Developer + UI/UX спеціаліст  
**Пріоритет**: ВИСОКИЙ (інтеграційний)  
**Час виконання**: 6-8 днів

### Технічне завдання:
Створити повнофункціональний CLI інтерфейс з відмінним UX.

### Архітектура класу:
```python
class PersonalAssistantCLI:
    storage: FileStorage
    contact_manager: ContactManager  
    note_manager: NoteManager
    command_matcher: CommandMatcher
```

### Категорії методів:

#### UI/UX методи:
- Кольоровий вивід (colorama)
- Форматовані заголовки та розділювачі
- Типізовані повідомлення (success/error/warning/info)
- Прогрес-бари для довгих операцій

#### Команди контактів (6 методів):
- `add_contact_command()` - покроковий ввід
- `search_contact_command()` - з підсвічуванням
- `show_contacts_command()` - з сортуванням  
- `edit_contact_command()` - selective editing
- `delete_contact_command()` - з підтвердженням
- `birthdays_command()` - з красивим виводом

#### Команди нотаток (6 методів):
- `add_note_command()` - мультирядковий ввід
- `search_notes_command()` - повнотекстовий пошук
- `show_notes_command()` - з пагінацією
- `edit_note_command()` - flexible editing
- `delete_note_command()` - з підтвердженням  
- `notes_by_tags_command()` - з автодоповненням

#### Системні команди:
- `statistics_command()` - детальні звіти
- `help_command()` - контекстна довідка
- `exit_command()` - graceful shutdown

### UX вимоги:
- ✅ Інтуїтивні підказки та підтвердження
- ✅ Graceful обробка Ctrl+C
- ✅ Збереження контексту між командами
- ✅ Автоматичне угадування команд
- ✅ Кольоровий вивід для покращення читабельності
- ✅ Підтримка української/англійської мов

### Критерії прийняття:
- [ ] Всі 13 команд працюють бездоганно
- [ ] Розумне угадування команд з пропозиціями
- [ ] Красивий кольоровий інтерфейс
- [ ] Robust error handling без crashes
- [ ] Мультимовна підтримка
- [ ] Автозбереження після кожної операції

---

## 🧪 ЗАВДАННЯ #10: ТЕСТУВАННЯ
**Файл**: `tests/test_main.py`  
**Розробник**: QA Engineer або Senior Developer  
**Пріоритет**: ВИСОКИЙ  
**Час виконання**: 4-5 днів

### Технічне завдання:
Створити повне покриття unit тестами всіх компонентів.

### Тест класи:

#### TestContact (6+ тестів):
- Створення контакту
- Додавання/видалення телефонів
- Додавання/видалення emails  
- День народження та розрахунки
- Серіалізація/десеріалізація
- Edge cases та валідація

#### TestNote (5+ тестів):
- Створення нотатки
- Управління тегами
- Пошук у змісті
- Оновлення метаданих
- Валідація полів

#### TestFileStorage (4+ тестів):
- Збереження/завантаження
- Backup та відновлення  
- Помилки файлової системи
- UTF-8 підтримка

#### TestContactManager (5+ тестів):
- CRUD операції
- Пошук та фільтрація
- Сортування
- Статистика
- Edge cases

#### TestNoteManager (5+ тестів):  
- CRUD операції
- Пошук за тегами
- Сортування
- Статистика тегів
- Індексація

#### TestCommandMatcher (4+ тестів):
- Точне розпізнавання
- Fuzzy matching
- Мультимовна підтримка
- Performance тести

### Технічні вимоги:
- ✅ Використання tempfile для ізоляції
- ✅ Mock'и для зовнішніх залежностей
- ✅ Покриття позитивних та негативних сценаріїв
- ✅ Performance тести для критичних операцій
- ✅ Інтеграційні тести основних workflows

### Критерії прийняття:
- [ ] Мінімум 90% code coverage
- [ ] Всі тести проходять стабільно
- [ ] Швидке виконання (< 30 сек для всіх тестів)
- [ ] Proper setup/teardown
- [ ] Зрозумілі assertion messages

---

## 📄 ЗАВДАННЯ #11: ДОПОМІЖНІ ФАЙЛИ
**Файли**: `main.py`, `setup.py`, `requirements.txt`  
**Розробник**: DevOps або Junior Developer  
**Пріоритет**: НИЗЬКИЙ  
**Час виконання**: 1 день

### main.py:
```python
from personal_assistant.cli import PersonalAssistantCLI

def main():
    assistant = PersonalAssistantCLI()
    assistant.run()

if __name__ == "__main__":
    main()
```

### setup.py:
- Package configuration
- Entry points для console scripts
- Dependencies specification
- Metadata (version, author, description)

### requirements.txt:
- colorama для кольорового виводу
- Інші залежності при необхідності

---

## 📅 ПЛАН ВИКОНАННЯ (5 тижнів)

### Тиждень 1: Фундамент
- 👤 Dev1: Завдання #1 (Field classes)
- 👤 Dev2: Завдання #2 (FileStorage)  
- 👤 Dev3: Завдання #8 (Validators)

### Тиждень 2: Моделі
- 👤 Dev1: Завдання #3 (Contact model)
- 👤 Dev2: Завдання #4 (Note model)

### Тиждень 3: Менеджери  
- 👤 Dev1: Завдання #5 (ContactManager)
- 👤 Dev2: Завдання #6 (NoteManager)
- 👤 Dev3: Завдання #7 (CommandMatcher)

### Тиждень 4: Інтерфейс
- 👤 Dev1+Dev2: Завдання #9 (CLI Interface)
- 👤 Dev3: Завдання #11 (Setup files)

### Тиждень 5: Тестування та інтеграція
- 👤 QA: Завдання #10 (Testing)
- 👤 Команда: Інтеграція, багфікси, документація

---

## ✅ КРИТЕРІЇ ГОТОВНОСТІ

### Для кожного завдання:
- [ ] Код відповідає PEP 8
- [ ] Повне покриття docstrings
- [ ] Unit тести написані та проходять
- [ ] Code review пройдено
- [ ] Інтеграція з іншими модулями працює

### Для всього проекту:
- [ ] Всі 13 команд працюють
- [ ] Дані зберігаються між сесіями
- [ ] Програма не падає при некоректному вводі
- [ ] Розумне угадування команд працює
- [ ] Інтерфейс інтуїтивний та зрозумілий

---

## 📚 РЕСУРСИ ДЛЯ РОЗРОБНИКІВ

### Документація:
- `TECHNICAL_REQUIREMENTS.md` - детальні технічні вимоги
- Готовий проект як reference implementation
- PEP 8 Style Guide
- Python typing documentation

### Інструменти:
- Python 3.9+
- VS Code з Python extension
- pytest для тестування
- black для форматування коду

### Контакти:
- Tech Lead: [ваш контакт]
- Code Review: [процес review]
- Issue tracking: [система трекінгу]

---

Кожне завдання може виконуватися незалежно з чіткими інтерфейсами між модулями. Це дозволяє ефективно розподілити роботу між командою та паралельно розвивати різні компоненти системи.