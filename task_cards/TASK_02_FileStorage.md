# 💾 TASK CARD #2: ФАЙЛОВЕ СХОВИЩЕ

**Розробник**: Backend Developer  
**Файл**: `personal_assistant/storage/file_storage.py`  
**Пріоритет**: 🔴 ВИСОКИЙ  
**Час**: 2-3 дні  
**Складність**: ⭐⭐⭐⭐

---

## 📋 ЗАВДАННЯ

Створити надійну систему збереження даних у JSON з автоматичним backup механізмом та відновленням при помилках.

## 🎯 МЕТА

Забезпечити:
- Безпечне збереження даних контактів та нотаток
- Автоматичне створення backup копій
- Відновлення при пошкодженні файлів
- Підтримку української мови (UTF-8)

## 📦 КЛАС FileStorage

```python
class FileStorage:
    """Надійне файлове сховище з backup системою."""
    
    def __init__(self, data_dir: str = "data") -> None:
        """Ініціалізація з створенням робочої папки."""
        
    def save_data(self, filename: str, data: Any) -> None:
        """Збереження даних з автоматичним backup."""
        
    def load_data(self, filename: str) -> Any:
        """Завантаження з відновленням з backup при помилках."""
        
    def file_exists(self, filename: str) -> bool:
        """Перевірка існування основного файлу."""
        
    def delete_file(self, filename: str) -> bool:
        """Видалення файлу та його backup."""
        
    def get_storage_info(self) -> Dict[str, Any]:
        """Інформація про стан сховища."""
        
    def clear_all_data(self) -> bool:
        """Очищення всіх даних (з підтвердженням)."""
```

## 🔧 ТЕХНІЧНІ ДЕТАЛІ

### Структура файлів:
```
data/
├── contacts.json          # Основні дані
├── contacts.json.backup   # Backup копія
├── notes.json
├── notes.json.backup
└── config.json
```

### Backup стратегія:
1. **Перед записом**: створити .backup копію поточного файлу
2. **Атомарний запис**: записати дані у тимчасовий файл, потім перейменувати
3. **При помилці читання**: автоматично відновити з .backup
4. **Перевірка цілісності**: валідація JSON перед фіналізацією

### Обробка помилок:
- **JSON Parse Error** → відновлення з backup
- **File Not Found** → створення порожнього файлу  
- **Permission Error** → інформативне повідомлення
- **Disk Full** → graceful degradation

## 📋 ДЕТАЛЬНІ МЕТОДИ

### save_data(filename: str, data: Any)
**Алгоритм**:
1. Створити backup поточного файлу (якщо існує)
2. Серіалізувати дані в JSON з UTF-8
3. Записати в тимчасовий файл (.tmp)
4. Атомарно перейменувати .tmp → основний файл
5. При помилці - відкотити зміни

**Особливості**:
- Підтримка складних типів через custom encoder
- Красиве форматування JSON (indent=2)
- UTF-8 encoding для української мови
- Atomic operations (або все, або нічого)

### load_data(filename: str) → Any
**Алгоритм**:
1. Спробувати завантажити основний файл
2. При помилці - спробувати backup файл
3. Якщо обидва пошкоджені - повернути порожню структуру
4. Логувати всі операції відновлення

**Повернення**:
- `{}` для contacts.json
- `[]` для notes.json
- `None` для інших файлів

### get_storage_info() → Dict[str, Any]
**Повертає**:
```python
{
    "data_directory": "/absolute/path/to/data",
    "files": {
        "contacts.json": {
            "exists": True,
            "size_bytes": 1024,
            "modified": "2024-01-15T10:30:00",
            "backup_exists": True,
            "backup_size": 1020
        },
        "notes.json": {...}
    },
    "total_files": 4,
    "total_size_bytes": 2048,
    "backup_coverage": 100.0  # % файлів з backup
}
```

## ✅ КРИТЕРІЇ ПРИЙНЯТТЯ

### Функціональні вимоги:
- [ ] Транзакційне збереження (atomic operations)
- [ ] Автоматичне створення та використання backup
- [ ] Відновлення при пошкодженні JSON
- [ ] UTF-8 підтримка для української мови
- [ ] Створення папок при необхідності

### Технічні вимоги:
- [ ] Type hints для всіх методів
- [ ] Docstrings з прикладами використання
- [ ] Логування важливих операцій
- [ ] Обробка всіх можливих винятків
- [ ] Покриття тестами мінімум 95%

### Якісні вимоги:
- [ ] Швидка робота (< 100ms для звичайних файлів)
- [ ] Мінімальне споживання пам'яті
- [ ] Читабельні повідомлення про помилки
- [ ] Graceful handling несподіваних ситуацій

## 🧪 ТЕСТОВІ СЦЕНАРІЇ

### Основні операції:
```python
storage = FileStorage("test_data")

# Збереження та завантаження
data = {"test": "значення", "number": 42}
storage.save_data("test.json", data)
loaded = storage.load_data("test.json")
assert loaded == data

# Перевірка існування
assert storage.file_exists("test.json")
assert not storage.file_exists("nonexistent.json")
```

### Backup та відновлення:
```python
# Створення backup
storage.save_data("test.json", {"version": 1})
storage.save_data("test.json", {"version": 2})

# Симуляція пошкодження основного файлу
with open("test_data/test.json", "w") as f:
    f.write("invalid json{")

# Автоматичне відновлення з backup
data = storage.load_data("test.json")
assert data["version"] == 1  # Відновлено з backup
```

### Edge cases:
```python
# Великі файли
big_data = {"items": list(range(10000))}
storage.save_data("big.json", big_data)

# Спеціальні символи
ukrainian_data = {"text": "Привіт, світ! 🇺🇦"}
storage.save_data("unicode.json", ukrainian_data)

# Порожні дані
storage.save_data("empty.json", {})
storage.save_data("empty_list.json", [])
```

### Обробка помилок:
```python
# Недоступна папка (simulation)
restricted_storage = FileStorage("/root/restricted")

# Пошкоджені файли
with open("test_data/broken.json", "w") as f:
    f.write("{broken json")

# Відсутні файли
data = storage.load_data("missing.json")
assert data == {}
```

## 🔗 ЗАЛЕЖНОСТІ

**Імпорти**:
- `json` для серіалізації
- `pathlib.Path` для роботи з файлами
- `typing` для type hints
- `datetime` для timestamps
- `logging` для логування операцій

**Використовується в**:
- ContactManager (завдання #5)
- NoteManager (завдання #6)
- CLI (завдання #9) - для статистики

## 📁 СТРУКТУРА КОДУ

```python
# Константи
DEFAULT_DATA_DIR = "data"
BACKUP_SUFFIX = ".backup"
TEMP_SUFFIX = ".tmp"

# Клас FileStorage
class FileStorage:
    def __init__(self, data_dir: str = DEFAULT_DATA_DIR):
        self.data_dir = Path(data_dir)
        self._ensure_data_dir()
    
    def _ensure_data_dir(self):
        """Створення робочої папки."""
        
    def _get_file_path(self, filename: str) -> Path:
        """Отримання повного шляху до файлу."""
        
    def _get_backup_path(self, filename: str) -> Path:
        """Отримання шляху до backup файлу."""
        
    def _create_backup(self, filename: str) -> bool:
        """Створення backup копії."""
        
    def _atomic_write(self, filepath: Path, data: str) -> None:
        """Атомарний запис файлу."""
```

## 🛡️ БЕЗПЕКА ТА НАДІЙНІСТЬ

### Атомарні операції:
1. Запис у .tmp файл
2. Sync на диск
3. Rename .tmp → основний файл
4. Видалення старого backup при успіху

### Відновлення при падінні:
- При перезапуску перевіряти наявність .tmp файлів
- Відкатувати незавершені операції
- Відновлювати з backup при потребі

### Валідація даних:
```python
def _validate_json(self, filepath: Path) -> bool:
    """Перевірка чи є файл валідним JSON."""
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            json.load(f)
        return True
    except (json.JSONDecodeError, IOError):
        return False
```

## 📚 РЕСУРСИ

- [JSON Best Practices](https://jsonapi.org/)
- [Python pathlib documentation](https://docs.python.org/3/library/pathlib.html)
- [Atomic file operations](https://stackoverflow.com/questions/2333872/atomic-writing-to-file-with-python)
- [UTF-8 handling in Python](https://docs.python.org/3/library/codecs.html)

## 🚀 ГОТОВНІСТЬ ДО ЗДАЧІ

### Checklist:
- [ ] Всі методи реалізовані
- [ ] Backup система працює
- [ ] UTF-8 підтримка перевірена
- [ ] Тести покривають edge cases
- [ ] Документація написана
- [ ] Code review пройдено

**Розблоковує завдання**: #5 (ContactManager), #6 (NoteManager)