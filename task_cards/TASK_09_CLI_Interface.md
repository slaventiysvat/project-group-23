# 🖥️ TASK CARD #9: CLI ІНТЕРФЕЙС

**Розробник**: Senior Developer + UI/UX спеціаліст  
**Файл**: `personal_assistant/cli.py`  
**Пріоритет**: 🔴 ВИСОКИЙ (інтеграційний)  
**Час**: 6-8 днів  
**Складність**: ⭐⭐⭐⭐⭐

---

## 📋 ЗАВДАННЯ

Створити повнофункціональний CLI інтерфейс що об'єднує всі компоненти в єдину зручну програму з відмінним користувацьким досвідом.

## 🎯 МЕТА

Надати користувачу:
- Інтуїтивний інтерфейс для всіх операцій
- Розумне угадування команд
- Красивий кольоровий вивід
- Robust обробку помилок
- Мультимовну підтримку

## 🏗️ АРХІТЕКТУРА КЛАСУ

```python
class PersonalAssistantCLI:
    """Головний CLI інтерфейс персонального помічника."""
    
    def __init__(self) -> None:
        """Ініціалізація всіх компонентів."""
        self.storage = FileStorage()
        self.contact_manager = ContactManager(self.storage)
        self.note_manager = NoteManager(self.storage)
        self.command_matcher = CommandMatcher()
        self.running = True
    
    def run(self) -> None:
        """Основний цикл програми."""
        
    def process_command(self, user_input: str) -> None:
        """Обробка користувацької команди."""
```

## 🎨 UI/UX КОМПОНЕНТИ

### Кольорова схема (colorama):
```python
class Colors:
    SUCCESS = Fore.GREEN      # Успішні операції
    ERROR = Fore.RED         # Помилки
    WARNING = Fore.YELLOW    # Попередження  
    INFO = Fore.CYAN        # Інформаційні повідомлення
    HEADER = Fore.MAGENTA   # Заголовки
    PROMPT = Fore.BLUE      # Підказки для вводу
    RESET = Style.RESET_ALL # Скидання
```

### Форматування виводу:
```python
def print_header(self, text: str) -> None:
    """Красивий заголовок з рамкою."""
    width = 60
    print(f"\n{Colors.HEADER}{'=' * width}")
    print(f"  {text.center(width - 4)}")
    print(f"{'=' * width}{Colors.RESET}\n")

def print_success(self, message: str) -> None:
    """Повідомлення про успіх з іконкою."""
    print(f"{Colors.SUCCESS}✅ {message}{Colors.RESET}")

def print_error(self, message: str) -> None:
    """Повідомлення про помилку з іконкою."""
    print(f"{Colors.ERROR}❌ {message}{Colors.RESET}")
```

### Інтерактивні елементи:
```python
def get_user_input(self, prompt: str, required: bool = True) -> str:
    """Отримання вводу з валідацією."""
    
def get_confirmation(self, message: str) -> bool:
    """Підтвердження дії (y/n)."""
    
def select_from_list(self, items: List[str], prompt: str) -> Optional[int]:
    """Вибір зі списку з номерами."""
```

## 📞 КОМАНДИ КОНТАКТІВ

### 1. add_contact_command():
**Алгоритм**:
1. Покрокове введення даних:
   - Ім'я (обов'язкове)
   - Телефони (можна декілька)
   - Email адреси (опційно)
   - День народження (опційно)
   - Адреса (опційно)
2. Валідація кожного поля
3. Підтвердження перед збереженням
4. Збереження та повідомлення про успіх

**Особливості**:
- Можливість додати декілька телефонів/emails
- Підказки про формат вводу
- Можливість пропустити опційні поля

### 2. search_contact_command():
**Алгоритм**:
1. Запит пошукового терміну
2. Пошук в менеджері контактів
3. Показ результатів з підсвічуванням
4. Можливість вибрати контакт для детального огляду

**Особливості**:
- Пошук за всіма полями
- Підсвічування знайдених термінів
- Показ релевантності результатів

### 3. show_contacts_command():
**Алгоритм**:
1. Отримання всіх контактів
2. Вибір сортування (ім'я, дата створення)
3. Форматований вивід зі статистикою
4. Пагінація для великої кількості

### 4. edit_contact_command():
**Алгоритм**:
1. Пошук контакту для редагування
2. Показ поточних даних
3. Selective editing - вибір що редагувати
4. Новий ввід з валідацією
5. Підтвердження змін

### 5. delete_contact_command():
**Алгоритм**:
1. Пошук контакту
2. Показ даних контакту
3. Подвійне підтвердження видалення
4. Видалення та повідомлення

### 6. birthdays_command():
**Алгоритм**:
1. Запит кількості днів наперед (за замовчуванням 7)
2. Отримання майбутніх днів народження
3. Красивий календарний вивід
4. Сортування за датами

## 📝 КОМАНДИ НОТАТОК

### 1. add_note_command():
**Особливості**:
- Мультирядковий ввід тексту
- Додавання тегів через кому
- Попередній перегляд перед збереженням

### 2. search_notes_command():
**Особливості**:
- Full-text пошук по змісту
- Пошук за заголовками
- Підсвічування знайдених слів

### 3. show_notes_command():
**Особливості**:
- Пагінація результатів (по 5-10 нотаток)
- Компактний та детальний режими перегляду
- Сортування за різними критеріями

### 4. notes_by_tags_command():
**Особливості**:
- Автодоповнення існуючих тегів
- AND/OR логіка пошуку
- Статистика використання тегів

## 🎛️ СИСТЕМНІ КОМАНДИ

### help_command():
**Контекстна довідка**:
```python
def help_command(self, specific_command: str = None) -> None:
    """
    Показує загальну довідку або детальну інформацію про конкретну команду.
    """
    if specific_command:
        # Детальна довідка про команду
        self.show_command_help(specific_command)
    else:
        # Загальний список команд з описами
        self.show_general_help()
```

### statistics_command():
**Детальна статистика**:
- Кількість контактів/нотаток
- Розподіл за типами даних
- Найпопулярніші теги
- Статистика використання команд
- Розмір файлів даних

## 🔄 ОСНОВНИЙ ЦИКЛ

```python
def run(self) -> None:
    """Головний цикл програми."""
    self.show_welcome()
    
    while self.running:
        try:
            # Отримання команди від користувача
            user_input = input(f"{Colors.PROMPT}📱 Введіть команду: {Colors.RESET}").strip()
            
            if not user_input:
                continue
                
            # Розпізнавання команди
            command, confidence = self.command_matcher.find_best_command(user_input)
            
            # Логіка виконання залежно від confidence
            if confidence >= 0.85:
                self.execute_command(command)
            elif confidence >= 0.4:
                self.suggest_alternatives(user_input, command, confidence)
            else:
                self.show_help_for_unclear_input(user_input)
                
        except KeyboardInterrupt:
            self.handle_ctrl_c()
        except Exception as e:
            self.handle_unexpected_error(e)
    
    self.show_goodbye()
```

## 🛡️ ОБРОБКА ПОМИЛОК

### Типи помилок:
```python
def handle_validation_error(self, error: ValueError) -> None:
    """Помилки валідації користувацького вводу."""
    
def handle_not_found_error(self, item_type: str, search_term: str) -> None:
    """Помилки коли щось не знайдено."""
    
def handle_storage_error(self, error: Exception) -> None:
    """Помилки файлової системи."""
    
def handle_unexpected_error(self, error: Exception) -> None:
    """Несподівані помилки з логуванням."""
```

### Graceful degradation:
- При помилках збереження - показати що дані в пам'яті
- При помилках завантаження - запропонувати відновлення
- При некоректному вводі - показати підказки

## 🌐 МУЛЬТИМОВНА ПІДТРИМКА

### Переключення мови:
```python
class Language:
    UK = "uk"
    EN = "en"

def set_language(self, lang: str) -> None:
    """Зміна мови інтерфейсу."""
    
def get_text(self, key: str) -> str:
    """Отримання тексту відповідно до поточної мови."""
```

## ✅ КРИТЕРІЇ ПРИЙНЯТТЯ

### Функціональні вимоги:
- [ ] Всі 13 команд реалізовані та працюють
- [ ] Розумне угадування команд з пропозиціями
- [ ] Robust обробка всіх типів помилок
- [ ] Автозбереження після кожної операції
- [ ] Graceful shutdown при Ctrl+C

### UI/UX вимоги:
- [ ] Кольоровий інтерфейс покращує читабельність
- [ ] Інтуїтивні підказки та підтвердження
- [ ] Швидка навігація між функціями
- [ ] Відповідність принципам good CLI design
- [ ] Accessibility (можна відключити кольори)

### Технічні вимоги:
- [ ] Модульна архітектура з чистими інтерфейсами
- [ ] Type hints та comprehensive docstrings
- [ ] Покриття тестами головних сценаріїв
- [ ] Performance оптимізації для великих даних
- [ ] Proper resource management

## 🧪 ТЕСТОВІ СЦЕНАРІЇ

### Основні workflows:
```python
# Симуляція користувацької сесії
def test_full_workflow():
    cli = PersonalAssistantCLI()
    
    # Додавання контакту
    with mock_input(["додай контакт", "Іван Петров", "+380501234567", "", "", "", "y"]):
        cli.process_command("додай контакт")
    
    # Пошук
    with mock_input(["пошук контакт", "Іван"]):
        cli.process_command("знайди контакт")
    
    # Статистика
    cli.process_command("статистика")
```

### Edge cases:
```python
# Некоректний ввід
def test_error_handling():
    # Пусті команди
    cli.process_command("")
    
    # Неіснуючі команди
    cli.process_command("невідома команда")
    
    # Помилки валідації
    with mock_input(["додай контакт", "", "invalid_phone"]):
        cli.process_command("додай контакт")
```

## 📁 СТРУКТУРА ФАЙЛУ

```python
# Imports та константи
from typing import Optional, List, Dict, Any
import sys
from colorama import init, Fore, Style

# Клас кольорів
class Colors:
    # ... константи кольорів

# Головний клас CLI
class PersonalAssistantCLI:
    def __init__(self): ...
    
    # UI методи
    def print_header(self): ...
    def print_success(self): ...
    def get_user_input(self): ...
    
    # Команди контактів
    def add_contact_command(self): ...
    def search_contact_command(self): ...
    # ... інші команди контактів
    
    # Команди нотаток  
    def add_note_command(self): ...
    # ... інші команди нотаток
    
    # Системні команди
    def help_command(self): ...
    def statistics_command(self): ...
    def exit_command(self): ...
    
    # Утиліти
    def execute_command(self): ...
    def suggest_alternatives(self): ...
    def handle_error(self): ...
    
    # Головний цикл
    def run(self): ...
```

## 🔗 ЗАЛЕЖНОСТІ

**Всі попередні завдання**:
- Field classes (завдання #1)
- FileStorage (завдання #2)
- Contact model (завдання #3)
- Note model (завдання #4)
- ContactManager (завдання #5)
- NoteManager (завдання #6)
- CommandMatcher (завдання #7)

**Зовнішні залежності**:
- `colorama` для кольорів
- `typing` для type hints

## 🚀 ГОТОВНІСТЬ ДО ЗДАЧІ

### Checklist:
- [ ] Всі команди працюють без помилок
- [ ] UI/UX відповідає вимогам
- [ ] Error handling comprehensive
- [ ] Performance acceptable
- [ ] Integration тести проходять
- [ ] Documentation повна

**Фінальний компонент** - об'єднує всю систему в робочу програму!